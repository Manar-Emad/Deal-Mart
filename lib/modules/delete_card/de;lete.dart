import 'package:flutter/material.dart';
class DeleteCart extends StatelessWidget {
  const DeleteCart({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(

      ),
    );
  }
}
