import 'package:flutter/material.dart';
class WalletBallenceScreen extends StatelessWidget {
  const WalletBallenceScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
